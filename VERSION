1.8

# Version history
# ---------------
# 1.8    Docker support: Dockerfile, docker-compose, GitHub Actions workflow, SPA serving
# 1.7    Documentation updates: corrected screen docs for Firewall, Images, Costs, Labels, Configure, Clone, Workflow
# 1.6    Documentation link in sidebar; mkdocs served at /manual with Security Fabric theme
# 1.5    Shutdown auth retry dialog; license server via FS API only; Configure op reorder + task waits; bulk ops skip wrong-state; nav reorder
# 1.4    Settings preferences moved left; Configure API call order matches UI; wait for registration task before fabric ops; friendly DNS error on template fetch
# 1.3    Bulk start/stop/shutdown skip wrong-state instances; already running/stopped toasts
# 1.2    Shutdown auth popup (skip/cancel/use-for-all); bulk bar clears immediately; LogStream fixed-height scrolling matches widget; Configure item reorder; label/hint text fixes
# 1.1    Shutdown via Fabric Studio API with DNS cleanup; Clone/Labels half-width layout; toast grammar fixes; README updates
# 1.0    README updates (Configure screen added, multi-key, feature order matches UI); Configure screen: Install header above radios, radio/None/X center-aligned
# 0.106  Configure screen: radio buttons red in Security Fabric theme, blue otherwise; fix None radio alignment
# 0.105  Configure screen: Fabric Workspace section — create multiple fabrics from templates, install selector (radio with None default), hostname field, SSH key management fixes, info icon on registration token
# 0.104  Configure screen: set hostname with {count} template (maps to instance number); info icon on registration token field
# 0.103  Configure screen: SSH public key management — add/remove multiple keys, optional "delete existing keys" checkbox; settings key always included; backed by Fabric Studio /api/v1/system/account/ssh/keys API
# 0.102  Configure screen: selection summary compresses contiguous ranges per base name, even when mixed with other instances (e.g. "fs-tve-devtest-000 to fs-tve-devtest-002, srv-tve-license-001")
# 0.101  Instances: add 5s delayed refresh after each mutation
# 0.100  Configure screen: SSH-style instance selectors (by name / by range), bulk configure, selection summary, split into 2 sections; Build screen cleanup
# 0.99  Settings: two-column layout (keys + appearance left, preferences right) on wide screens
# 0.98  Multi-key GCP support: upload/manage multiple service account keys, grouped project selector, switch-project dialog on key upload, inline key rename
# 0.97  Configure golden image: wait for instance readiness before connecting (polls /api/v1/session/check, retries every 10s up to 5 min)
# 0.96  Configure golden image: Fabric Studio API integration (auth, license server, registration token, password change), Settings default admin password
# 0.95  Build screen UX (section locking, DNS, labels/tags), FQDN fix (no zero-pad, include product), remove -000 deletion guard, instance table single-line rows
# 0.94  Clone workshop naming, DNS FQDN includes workshop name
# 0.93  Split Build screen into create/configure sections, auto-detect subnetwork on build
# 0.92  Build screen overhaul: zone/machine type/image dynamic dropdowns, searchable selects, instance name preview, label updates, nav reorder; zone locations from conf file with auto-discovery; group filter in instances
# 0.91  Display original key filename in Settings UI, add key location hint in upload drop zone
# 0.90  Fix live SKU pricing (E2 family), auto-select project on key upload, clear project on key delete, invalidate caches on key change
# 0.89  Grey out nav items when no service account key is configured, fix React Router future flag warning
# 0.88  Add Costs page, estimated hourly cost and instance specs in detail popup, dynamic machine type/zone dropdowns
# 0.87  README updates
# 0.86  Add README
# 0.85  SSH and Build output panels wrapped in widget boxes, output area fills full panel height
# 0.84  Fix Configurations screen padding and layout to match other screens
# 0.83  Instances: purpose filter, SF theme styling, Edit Labels shortcut, Clone: purpose label, name field, cross-zone, zones dropdown
# 0.82  Clone screen: custom name field, cross-zone support, destination zone dropdown, fix source dropdown re-open
# 0.81  Auto-fit sidebar width to project name, close About dialog by clicking outside
# 0.80  Small UX change: use Fortinet red highlight for Security Fabric theme selection in Appearance widget
# 0.79  Fill zone field only after source instance is selected in Clone screen
# 0.78  Add clickable version number that opens About dialog with credits
# 0.77  Fix SSH live streaming for single host, add command logging, fix sleep position visibility, fix Load all IPs type mismatch
# 0.76  Add Configurations screen with file editor, config-based SSH execution, menu reordering and UX improvements
# 0.75  Merge Dependabot PR #2: upgrade vite 5→8, fix SSH.tsx public IPs type error
# 0.74  Rename 'Select by range' to 'Select instances by range...' in SSH screen
# 0.73  Fix version display showing full VERSION file content instead of just the version number
# 0.72  Add version history tracking to VERSION file
# 0.71  Add key file delete button, clickable 'No key configured' link, fix project selector colors in Security Fabric theme
# 0.70  Fix backend security vulnerabilities (fastapi, python-multipart, google-cloud packages) and remove obsolete start.sh
# 0.69  Security Fabric theme refinements, FQDN in instances, custom dropdowns, cross-platform setup/start/stop scripts
# 0.68  Initial release
